"use client";
import { useState } from "react";
import Link from "next/link";
import { useJourney } from "../journey/JourneyProvider";
import { calculateApplicationProgress, getApplicationById, getRoleById } from "../journey/selectors";
import { ConfirmDialog, Progress, StatusBadge } from "../ui/Primitives";
import { DraftStudio } from "./DraftStudio";
export function ApplicationWorkspacePage({applicationId}:{applicationId:string}){const {state,dispatch}=useJourney(); const [tab,setTab]=useState<"cv"|"cover"|"preflight">("cv"); const [confirm,setConfirm]=useState(false); const app=getApplicationById(state,applicationId); const role=app&&getRoleById(state,app.roleId); if(!app||!role)return <section className="career-container cj-empty"><h1>Application not found</h1><Link href="/applications">Back to applications</Link></section>; const checks=Object.entries(app.preflight); return <section className="career-container cj-page-head"><Link href="/applications">← Applications</Link><h1>{role.title} application</h1><p>{role.company} · {role.location}</p><StatusBadge status={app.status}/><Progress value={calculateApplicationProgress(app)} label="Application progress"/><div className="cj-stepper" aria-label="Progress steps">{["Review requirements","Tailor CV","Tailor cover letter","Complete pre-flight","Confirm submitted"].map((s,i)=><span key={s}>✓ {i+1}. {s}</span>)}</div><div className="career-doc-switch"><button aria-pressed={tab==="cv"} onClick={()=>setTab("cv")}>CV</button><button aria-pressed={tab==="cover"} onClick={()=>setTab("cover")}>Cover letter</button><button aria-pressed={tab==="preflight"} onClick={()=>setTab("preflight")}>Pre-flight</button></div><div className="cj-workspace"><div>{tab!=="preflight"?<DraftStudio role={role} application={app} kind={tab} onChange={html=>dispatch({type:"updateArtifact",applicationId:app.id,kind:tab,html})}/>:<article className="cj-card"><h2>Pre-flight checklist</h2>{checks.map(([k,v])=><label className="cj-check" key={k}><input type="checkbox" checked={v} onChange={()=>dispatch({type:"togglePreflight",applicationId:app.id,key:k})}/>{k}</label>)}<p>External application link readiness is only a placeholder; CareerAI will not submit for you.</p><button className="career-btn" onClick={()=>setConfirm(true)}>Mark as submitted</button></article>}</div><aside className="cj-card"><h2>Requirements and evidence</h2>{role.requirements.map(r=><p key={r.label}>{r.coverage}: {r.label}</p>)}<h2>Activity timeline</h2><ol>{app.timeline.map(e=><li key={e.id}>{e.label}: {e.detail}</li>)}</ol><h2>Notes</h2><textarea value={app.notes} onChange={e=>dispatch({type:"updateApplicationNotes",applicationId:app.id,notes:e.target.value})}/></aside></div><ConfirmDialog open={confirm} title="Mark as submitted?" description="Confirm only after you submitted through the employer or job board. CareerAI did not submit externally and will only update local tracking." confirmLabel="Mark submitted" onCancel={()=>setConfirm(false)} onConfirm={()=>{dispatch({type:"markSubmitted",applicationId:app.id});setConfirm(false)}}/></section>}
